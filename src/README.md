# src/

Next.js アプリケーションのコードを配置します。

セットアップ手順は [docs/planning/01-static-site-setup.md](../docs/planning/01-static-site-setup.md) を参照してください。

```bash
# Next.js プロジェクトをこのフォルダに初期化
npx create-next-app@latest . --typescript --tailwind --app
```
